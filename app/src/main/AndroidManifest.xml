<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.host.imagegallery">

    <uses-sdk
        android:minSdkVersion="18"
        android:targetSdkVersion="24"
        />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".resimlistesi"
            android:label="@string/fotolarim"
            android:theme="@style/AppTheme.NoActionBar"
             />
        <activity
            android:name=".kayit"
            android:label="@string/title_activity_resimlistesi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".girisEkrani"
            android:label="@string/title_activity_resimlistesi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UploadActivity"
            android:label="@string/title_activity_resimlistesi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".eski.ImageGallery"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|uiMode"
            android:screenOrientation="portrait"/>

    </application>

</manifest>